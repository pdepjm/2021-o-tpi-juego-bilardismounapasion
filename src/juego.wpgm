import wollok.game.*
import cartas.*
import texto.*
import partida.*

program juego {
	game.cellSize(40)
	game.height(20)
	game.width(30)
	game.title("Pdep-Jitsu")
	game.boardGround("Fondo.png")
	partida.iniciar()
	game.start()
}