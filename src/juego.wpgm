import wollok.game.*
import cartas.*
import texto.*

program juego {
	game.cellSize(40)
	game.height(20)
	game.width(30)
	game.title("Pdep-Jitsu")
	game.ground("Fondo.png")
	partida.iniciar()
	game.start()
}